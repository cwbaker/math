
buildfile "math_lua/math_lua.build";
buildfile "math_test/math_test.build";

local settings = build.current_settings();
for _, architecture in ipairs(settings.architectures) do
    build.Library ("math", architecture) {
        project_files = {
            "%.[chi]pp"
        };
    };
end
