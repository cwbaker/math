
buildfile "math_lua/math_lua.build";
buildfile "math_test/math_test.build";

for _, forge in forge:default_builds("cc_.*") do
    forge:Library '${lib}/math' {
        project_files = {
            "%.[chi]pp"
        };
    };
end
