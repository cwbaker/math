
buildfile 'math_lua/math_lua.forge';
buildfile 'math_test/math_test.forge';

for _, toolset in toolsets('cc_.*') do
    toolset:StaticLibrary '${lib}/math' {
        project_files = {
            "%.[chi]pp"
        };
    };
end
